{
  "name": "vertexhub",
  "version": "0.1.0",
  "private": true,
  "description": "Organizational Nervous System - Eliminate organizational blindness",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
